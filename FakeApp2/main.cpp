#include <windows.h>

int WINAPI WinMain(
    HINSTANCE hInstance,
    HINSTANCE hPrevInstance,
    LPSTR lpCmdLine,
    int nCmdShow)
    {
        while (1)
        {
            //Infinite loop to let run the exe
            //You need to kill the process manually
        }
    }

